"""Golden-fixture generators for tests/fixtures/ (M4-T02).

Every subcommand writes byte-identical output on re-run under the pinned
requirements.txt: fixed seeds, single-threaded CPU math, sorted-key
serialization, pinned hub revisions, no timestamps.
"""
