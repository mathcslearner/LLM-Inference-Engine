// Placeholder translation unit: keeps the engine_sampling archive non-empty until
// this module gains real sources (see ROADMAP.md).
namespace engine::sampling {

void module_anchor() noexcept {}

}  // namespace engine::sampling
